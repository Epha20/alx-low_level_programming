#include <stdio.h>
#include <stdlib.h>
#include "art.h"

int add(int a, int b)
{
	int nadd = a+b;
	return nadd;
}
int sub(int a, int b)
{
	int nsub = a-b;
	return nsub;
}
int mul(int a, int b)
{
	int nmul = a*b;
	return nmul;
}
int divi(int a, int b)
{
	int ndiv = a/b;
	return ndiv;
}
int mod(int a, int b)
{
	int nmod = a%b;
	return nmod;
}
